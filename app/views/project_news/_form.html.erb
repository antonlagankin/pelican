<%= form_for(@project_news) do |f| %>
  <% if @project_news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_news.errors.count, "error") %> prohibited this project_news from being saved:</h2>

      <ul>
      <% @project_news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :tags %><br>
    <%= f.text_field :tags %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
