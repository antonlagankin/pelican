<%= form_for(@about_project) do |f| %>
  <% if @about_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@about_project.errors.count, "error") %> prohibited this about_project from being saved:</h2>

      <ul>
      <% @about_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :decription %><br>
    <%= f.text_field :decription %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
